---
context: deployment
status: in_progress
last_updated: 2026-07-20T22:58:09+05:30
---

# OmniDraft — Continue Here

## Current State
Deploying OmniDraft to AWS. App Runner requires subscription sign-up. Elastic Beanstalk failed (t2.micro not free-tier eligible). EC2 instance `i-03361182a24148801` (t3.micro) is running at **3.92.56.21** with Docker installed, but the container keeps restarting due to nginx PID file permission (`omnidraft` user can't write `/run/nginx.pid`).

**Fix applied locally** — Dockerfile and nginx.conf updated:
- Added `pid /tmp/nginx.pid;` to `docker/nginx.conf`
- Added `/run` and `/var/cache/nginx` to `chown` in Dockerfile
- Rebuilt and pushed image `028417007474.dkr.ecr.us-east-1.amazonaws.com/omnidraft:latest`

## Completed
- [x] Full project spec (OMNIDRAFT-SPEC.md)
- [x] All prompt files 1-7
- [x] Frontend code (React + Vite + Tailwind + Zustand)
- [x] Backend code (FastAPI + Supabase + NVIDIA API)
- [x] SQL schema migrated to Supabase (4 tables, RLS, 12 seed templates)
- [x] Fix: Vite proxy (3001→8000), SSE parsing, CORS config
- [x] Docker multi-stage build (frontend build + backend deps + nginx)
- [x] ECR repo created, image pushed
- [x] EC2 instance `i-03361182a24148801` (t3.micro) launched
- [x] IAM role `omnidraft-ec2-role` with ECR read + SSM access
- [x] Security group `sg-0420eb9111e750bd8` with port 8080 open
- [x] SSM agent online and functional

## Work Remaining
1. **SSH or SSM into EC2**, stop old container, pull new image, run fresh container
2. **Verify** `http://3.92.56.21:8080/api/health` returns 200
3. **Update ALLOWED_ORIGINS** to include `http://3.92.56.21:8080`
4. **Enable magic link auth** in Supabase Dashboard → Auth → Settings (set Site URL)
5. **Create `uploads` storage bucket** in Supabase Dashboard → Storage
6. **Clean up** Elastic Beanstalk application `omnidraft` (stuck in Launching)

## Decisions Made
- Used AWS EC2 (not App Runner or EB) because App Runner needs subscription and EB had instance type issues
- Using t3.micro which is free-tier eligible
- Single-container architecture: nginx serves frontend + proxies API to uvicorn
- Running as `omnidraft` user (non-root) — need to ensure nginx dirs are writable

## Blockers
- None remaining; the nginx PID fix is built into the latest image

## Key Credentials
- **EC2 IP**: 3.92.56.21
- **EC2 Instance**: i-03361182a24148801
- **EC2 Key**: C:\Users\amalc\AppData\Local\Temp\omnidraft-ec2.pem
- **ECR Image**: 028417007474.dkr.ecr.us-east-1.amazonaws.com/omnidraft:latest
- **Supabase URL**: https://fegbaqkmjqxiphsndodk.supabase.co
- **Supabase PAT**: sbp_PAT_REMOVED_REMOVED
- **NVIDIA Model**: z-ai/glm-5.2
- **AWS Account**: 028417007474
- **AWS Region**: us-east-1

## Resume Command
```bash
python C:\Users\amalc\AppData\Local\Temp\redeploy.py
```
Or via SSM:
1. `docker stop $(docker ps -q); docker rm $(docker ps -aq)`
2. `aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 028417007474.dkr.ecr.us-east-1.amazonaws.com`
3. `docker pull 028417007474.dkr.ecr.us-east-1.amazonaws.com/omnidraft:latest`
4. `docker run -d --restart unless-stopped -p 8080:8080 -e SUPABASE_URL=... -e SUPABASE_SERVICE_KEY=... -e NVIDIA_API_KEY=... -e NVIDIA_MODEL=z-ai/glm-5.2 -e ALLOWED_ORIGINS="http://localhost:5173,http://3.92.56.21:8080" -e RATE_LIMIT=20/minute -e MAX_TOKENS=4096 -e LOG_LEVEL=INFO 028417007474.dkr.ecr.us-east-1.amazonaws.com/omnidraft:latest`
5. `curl http://localhost:8080/api/health`
