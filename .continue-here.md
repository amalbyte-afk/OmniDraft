---
context: deployment
status: live
last_updated: 2026-07-22
---

# OmniDraft — Live on Elastic Beanstalk

## Current State
Deployed on AWS Elastic Beanstalk (single-instance, t3.micro) via Docker platform. Container built from ECR image `028417007474.dkr.ecr.us-east-1.amazonaws.com/omnidraft:latest`.

## URLs
- **App**: http://omnidraft-prod.eba-pepktrup.us-east-1.elasticbeanstalk.com
- **Health**: http://omnidraft-prod.eba-pepktrup.us-east-1.elasticbeanstalk.com/api/health
- **EC2 (old, terminated)**: 3.92.56.21

## Env Vars Configured
SUPABASE_URL, SUPABASE_SERVICE_KEY, NVIDIA_API_KEY, NVIDIA_MODEL, ALLOWED_ORIGINS, RATE_LIMIT, MAX_TOKENS, LOG_LEVEL

## Completed
- [x] Full project built (React + FastAPI + NVIDIA API + Supabase)
- [x] Docker multi-stage build + ECR push
- [x] EB environment `omnidraft-prod` — READY (Green)
- [x] App version v2 deployed (Dockerrun.aws.json → ECR image)
- [x] Old EC2 instance terminated
- [x] Root `.gitignore` created, `.env` files removed from git tracking

## Remaining
- [ ] **HTTPS**: Still HTTP only. Need CloudFront or certbot for SSL
- [ ] **Supabase Auth**: Enable magic link in Dashboard → Auth → Settings → Auth Providers → Email
- [ ] **Key rotation**: `backend/.env` with live credentials was in git history — should rotate keys
- [ ] **`.env.example` cleanup**: Contains `backend/.env.example` with placeholder values — fine but worth updating
